services:
  db:
    image: postgres:17
    environment:
      POSTGRES_USER: send
      POSTGRES_PASSWORD: send
      POSTGRES_DB: send
    ports:
      - '5433:5432'
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U send']
      interval: 5s
      timeout: 5s
      retries: 10
